import { Field } from 'payload/types';

export const internalTitle = [
    {
        name: 'internalTitle',
        type: 'text'
    }
] as Field[];

export const internalTitleAsTitle = {
    useAsTitle: 'internalTitle'
};
