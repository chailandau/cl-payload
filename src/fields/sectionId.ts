import { Field } from 'payload/types';

export const sectionId = [
    {
        name: 'sectionId',
        type: 'text',
        admin: {
            description: 'For use in anchor links'
        }
    }
] as Field[];
