import { Field } from 'payload/types';

export const testimonial = [
    {
        name: 'testimonial',
        type: 'richText',
        admin: {
            elements: [],
            leaves: []
        }
    }
] as Field[];
